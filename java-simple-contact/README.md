# Contact Form (Java GUI)

Simple Swing application with a form to input **Name**, **Email**, and **Phone**, then display entries in a table.

## Run

From the `contactform` folder:

```bash
javac -d bin src/App.java
java -cp bin App
```

Or open the folder in VS Code / Cursor and run `App.java` with the Java extension.
